 Command line instructions
Git global setup

git config --global user.name "Roberto Vinicius Kuo"
git config --global user.email "viniciuszxz@gmail.com"

Create a new repository

git clone http://viniciusrvk@projetos.imd.ufrn.br/viniciusrvk/Laboratorio3.git
cd Laboratorio3
touch README.md
git add README.md
git commit -m "add README"
git push -u origin master

Existing folder

cd existing_folder
git init
git remote add origin http://viniciusrvk@projetos.imd.ufrn.br/viniciusrvk/Laboratorio3.git
git add .
git commit
git push -u origin master

Existing Git repository

cd existing_repo
git remote add origin http://viniciusrvk@projetos.imd.ufrn.br/viniciusrvk/Laboratorio3.git
git push -u origin --all
git push -u origin --tags

